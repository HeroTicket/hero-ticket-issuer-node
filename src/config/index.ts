import config from './config.json';

export default {
    RHS_URL: config.rhs_url,
    CONTRACT_ADDRESS: config.contract_address,
    CIRCUITS_PATH: config.circuits_path,
    RPC_URL: config.rpc_url,
    WALLET_KEY: config.wallet_key,
};
